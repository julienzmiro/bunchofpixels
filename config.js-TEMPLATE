var config = {
development: {
    url: URL,
    database: {
        user:   USERNAME,
        pwd:    PASSWORD,
        host:   HOST,
        port:   PORT,
        db:     NAME
    },
    server: {
        host: '127.0.0.1',
        port: PORT
    },
    googleAuth: {
      clientID: CLIENTID,
      clientSecret: CLIENTSECRET',
      callbackURL: CALLBACKURL
    },
    figmaAuth: {
      clientID: CLIENTID,
      clientSecret: CLIENTSECRET,
      callbackURL: CALLBACKURL
    },
    session: {
      secret: SECRET,
      cookieSecret: SECRET
    }
},
production: {
    url: URL,
    database: {
        user:   USER,
        pwd:    PASSWORD,
        host:   HOST,
        port:   PORT,
        db:     NAME
    },
    server: {
        host:   HOST,
        port:   PORT
    },
    googleAuth: {
      clientID: CLIENTID,
      clientSecret: CLIENTSECRET',
      callbackURL: CALLBACKURL
    },
    figmaAuth: {
      clientID: CLIENTID,
      clientSecret: CLIENTSECRET,
      callbackURL: CALLBACKURL
    },
    session: {
      secret: SECRET,
      cookieSecret: SECRET
    }
}
};
module.exports = config;
